export interface IMesero {
  titulo: string;
  precio: string;
  descripcion: string;
  urlImg: string;
}

export interface ILetrero {
  titulo: string;
  descripcion: string,
  urlImg: string,
  active: boolean
}
